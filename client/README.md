# Client Application

This folder contains the React client for the budget tracker.

## Scripts

- `npm start`: runs the development server.
- `npm test`: runs the test suite.
- `npm run build`: builds the production bundle.

## Notes

- The client expects the server API at `http://localhost:5001`.
- Keep naming explicit and consistent.
- Use UK spelling in user-facing text.
